<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="X.Views.MainPage"
             xmlns:vm="clr-namespace:X.ViewModels"
             Title="Mattias Application">
    <ContentPage.BindingContext>
        <vm:MainViewModel/>
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions >
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Button x:Name="BtnNavigateToQuotes" TextColor="White" Grid.Column="0" Grid.Row="0" Text="Quotes"/>
            <Button x:Name="BtnNavigateToJokes" TextColor="White" Grid.Column="1" Grid.Row="0" Text="Jokes"/>
            <Button x:Name="BtnNavigateToLibrary" TextColor="White" Grid.Column="0" Grid.Row="1" Grid.ColumnSpan="2" Text="Library"/>

        </Grid>
    </ContentPage.Content>
</ContentPage>